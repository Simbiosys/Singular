<!DOCTYPE HTML>
<html>
  <head>
    {{> head}}
  </head>
  <body>
    {{> debug}}
    <main class="cuerpo">
      {{> navigation}}
      {{> cabecera}}
      <section class="notas">
        <form method="POST" action="{{page.host}}/notas/nuevo">
          <label for="nota" class="etiqueta">{{labels.note}}</label>
          <textarea class="campo-texto campo-nota" id="nota" name="nota" placeholder="{{labels.note}}"></textarea>
          <input type="submit" value="{{labels.save}}" class="btn-enviar" id="" name="" />
        </form>
      </section>
    </main>
    {{> pie}}
  </body>
</html>
